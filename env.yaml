name: mm_env
channels:
  - defaults
dependencies:
  - python=3.8
  - cudatoolkit=11.4
  - cudnn
  - cupy
  - matplotlib
  - ipython
  - numpy
  - numba
  - pip
  - scipy
  - tqdm
  - joblib=1.0.1
  - conda-forge::mrcfile
  - conda-forge::pyvista
  - conda-forge::gputil
  - schrodinger::pymol-bundle
  # pymol-bundle contains apbs and pdb2pqr; also pymol can be used to add hydrogens
  - pip:
    - voltools
#    - cupy-cuda114
